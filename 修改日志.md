## 2026-5-11
### A (完成)
1. 核心推荐采用AI
目前打算是用户输入->LLM进行优化，与数据库里的情绪标签进行匹配（通过一定的算法），然后将标签的参数传给查询地点的repository层进行查找

## 2026-5-14
比的就是用户query向量和每个地点向量之间的余弦相似度

向量数据库存地点描述文本的向量 + 地点的metadata（ID、名称）

只存官方库的地点向量。

|数据类型|	存在哪	|                为什么                 |
|:--:|:--:|:----------------------------------:|
官方库地点向量	|Redis向量库|        	全局共享，所有用户搜的都是同一批地点         |
用户去过/收藏的地点	|MySQL|user_place_memory 表	这是结构化数据，用SQL查更快 |
用户偏好画像	|MySQL|     user_preference 表	也是结构化数据      |
聊天记忆|	RedisChatMemoryRepository（你已有）|	LangChain4j的ChatMemory|
用户记忆不需要向量化。| 因为用户去过的地方就是你官方库里的地点，查 user_place_memory |拿到 place_id 列表，直接 JOIN place_emotion_library 就行，不需要再走向量检索。|